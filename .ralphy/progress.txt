# Ralphy Progress Log

## FE: Stats visualization with guess distribution
- Created `GuessDistribution` component with Wordle-style horizontal bar histogram
- Shows guess distribution (1-6) calculated from localStorage won games only
- CSS-only bars with proportional widths, green highlight for current game
- Zero-count bars show minimal 4px sliver for visual consistency
- Empty state with "No data yet" message when no completed games
- Added i18n translations (profile.guessDistribution, profile.noDataYet) in all 5 locales
- Integrated component into ProfilePanel below stats grid
- Set up Vitest test framework with jsdom and testing-library
- Wrote 8 tests covering: empty state, lost games filtering, bar rendering, current game highlighting, localStorage key filtering, zero-count bars
