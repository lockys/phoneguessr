# Ralphy Progress Log

## BE: Add database migration for new features
- Updated `phoneguessr/src/db/schema.ts`:
  - Added 4 nullable columns to `phones` table: `releaseYear`, `priceTier`, `formFactor`, `difficulty`
  - Created `phoneFacts` table (phone_id FK -> phones, fact_text, fact_type)
  - Created `streaks` table (user_id FK -> users, current_streak default 0, best_streak default 0, last_played_date)
  - Created `hints` table (user_id FK -> users, puzzle_id FK -> daily_puzzles, hint_type)
- Generated initial Drizzle migration: `phoneguessr/drizzle/0000_flaky_revanche.sql`
- Added schema validation test: `phoneguessr/src/db/schema.test.ts` (49 assertions, all passing)
- All new phone columns are nullable to preserve compatibility with existing seed data
