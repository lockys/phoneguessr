# Ralphy Progress Log

## FE: Build hint system UI (agent-12)

### Completed
- Created `HintButtons` component (`phoneguessr/src/components/HintButtons.tsx`)
  - 3 pill buttons: Brand, Year, Price (maps to hint types: brand, year, price_tier)
  - Max 2 hints per game with counter display ("1/2 hints used")
  - Used hints show revealed value inline with disabled styling
  - Penalty warning ("+15s per hint") shown before first use
  - Haptic feedback via web-haptics on hint use
  - Mock mode support (client-side hints) and API mode (POST /api/hint)
- Integrated HintButtons into Game component below PhoneAutocomplete (playing state only)
- Added CSS styles in `index.css`: pill buttons with accent border, hover/active states, used state styling
- Added i18n keys to all 5 locales (en, ja, ko, zh-TW, zh-CN): hint.brand, hint.year, hint.price, hint.used, hint.penaltyWarning
- Set up vitest + @testing-library/react test infrastructure
- Wrote 9 tests covering: render, penalty warning, mock reveal, haptic feedback, counter, max limit, disabled state, API call
- All tests passing, Biome lint clean on changed files

