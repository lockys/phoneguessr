# Ralphy Progress Log

## FE: Build yesterday's puzzle reveal UI
- Created `YesterdayReveal` component showing yesterday's phone (image, brand, model, release year)
- Added fun facts card list and community stats (players, win rate, avg guesses)
- Animate-in on first view using IntersectionObserver + CSS transitions
- Added to AboutPanel as the first section after the tagline
- Created BFF endpoint `GET /api/puzzle/yesterday` with mock mode support
- Added `getYesterdayPuzzle()` to puzzle lib for production mode
- Created Vercel serverless endpoint `api/puzzle/yesterday.ts`
- Added mock data functions (`getMockYesterdayPuzzle`, `getMockYesterdayImageData`) to state.ts
- i18n translations added for all 5 locales (en, zh-TW, zh-CN, ja, ko)
- Set up vitest with jsdom, @testing-library/react, wrote 9 tests (all passing)
- CSS styles added to index.css using existing design tokens
